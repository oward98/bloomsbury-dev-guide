import React from 'react';

export const VernacularPage = ({images, title}) => {
    return (
        <main>
            <header>
                <h1>{title}</h1>
            </header>
        </main>
    )
}